// LeadsTable — sortable, paginated table of verified leads.
// STUB — finalize in M6.
//
// Columns (see ../../../docs/architecture/data-model.md): handle, followers, median_er, clr,
// geo_confidence_score, niche, status (StatusBadge), contact_email/email_deliverable.
// Row click opens LeadDetailDrawer.

// TODO(M6): render rows from useLeads(); column sort; pagination.
export default function LeadsTable() {
  return null;
}
