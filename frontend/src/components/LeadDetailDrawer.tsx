// LeadDetailDrawer — full detail panel for a single lead.
// STUB — finalize in M6.
//
// Shows all verification metrics + classification labels + evidence (recent posts, geo signals).
// Opened from a LeadsTable row.

// TODO(M6): implement drawer; accept a lead + open/close state.
export default function LeadDetailDrawer() {
  return null;
}
