// ExportCsvButton — export the currently filtered leads to CSV.
// STUB — finalize in M6.
//
// Serializes the current (filtered) result set to CSV client-side and triggers a download.

// TODO(M6): accept rows; build CSV; trigger download.
export default function ExportCsvButton() {
  return null;
}
