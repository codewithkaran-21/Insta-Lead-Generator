// FilterSidebar — filter controls for the leads view.
// STUB — finalize in M6.
//
// Filters map to columns on the `leads` table (see ../../../docs/architecture/data-model.md):
//   niche, min median_er, follower band, geo_confidence tier, status (VERIFIED/GOLD),
//   contactable (email_deliverable).
// Emits a filter object consumed by useLeads().

// TODO(M6): implement controlled inputs + onChange(filters).
export default function FilterSidebar() {
  return null;
}
