// Supabase browser client (anon key only).
// STUB — finalize in M6.
//
// SECURITY: uses the ANON key exclusively. Never import or reference the service_role key here
// or in any NEXT_PUBLIC_ variable — it bypasses RLS. See ../../../docs/security.md.

// import { createClient } from "@supabase/supabase-js";

// const url = process.env.NEXT_PUBLIC_SUPABASE_URL!;
// const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;
// export const supabase = createClient(url, anonKey);

// TODO(M6): instantiate and export the typed browser client.
export {};
