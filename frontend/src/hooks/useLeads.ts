// useLeads — data-fetching hook: filters -> Supabase query -> leads.
// STUB — finalize in M6.
//
// Builds a PostgREST query from the filter object and returns { data, loading, error }.
// Read-only via the anon client (src/lib/supabase.ts); RLS restricts to SELECT.

// import { supabase } from "@/lib/supabase";

// TODO(M6): implement query builder + loading/error state (SWR or React Query optional).
export function useLeads(/* filters */) {
  throw new Error("TODO(M6): implement useLeads");
}
