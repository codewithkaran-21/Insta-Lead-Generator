// Dashboard page — composes the filter sidebar + leads table.
// STUB — finalize in M6. See ../../../docs/workflow.md (dashboard) and data-model.md.

// TODO(M6): wire FilterSidebar -> useLeads(filters) -> LeadsTable, with LeadDetailDrawer
// and ExportCsvButton. Read-only view of VERIFIED/GOLD leads via the anon Supabase client.
export default function DashboardPage() {
  return (
    <main>
      <h1>InstaLeads Dashboard</h1>
      {/* TODO(M6): <FilterSidebar /> + <LeadsTable /> */}
      <p>Scaffold placeholder — not yet implemented.</p>
    </main>
  );
}
