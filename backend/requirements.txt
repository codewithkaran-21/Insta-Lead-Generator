# InstaLeads backend — starter dependency pins.
# Versions are conservative floors known to work with Python 3.11; tighten to exact
# pins (==) once a lockfile / first successful run establishes the working set.
# See docs/api-integrations.md and docs/configuration.md.

# --- Data contract & validation ---
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0

# --- HTTP / providers ---
httpx>=0.27,<0.28
apify-client>=1.7,<2.0
groq>=0.9,<1.0

# --- Verification helpers ---
dnspython>=2.6,<3.0          # MX-record deliverability check (Stage 4 contact gate)
langdetect>=1.0.9,<2.0       # language signal for geo-confidence

# --- Persistence ---
supabase>=2.5,<3.0           # Supabase Python client (service_role writes)

# --- Ops ---
structlog>=24.1,<25.0        # structured JSON logging (docs/observability.md)
tenacity>=8.2,<9.0           # retry/backoff for rate-limited providers
python-dotenv>=1.0,<2.0      # load .env in local runs

# --- Dev / test (optional; consider a separate requirements-dev.txt) ---
# pytest>=8.0,<9.0
# pytest-asyncio>=0.23,<0.24
# respx>=0.21,<0.22           # httpx mocking for provider integration tests
